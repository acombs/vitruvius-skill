# Dress rehearsal — <demo name>

> Stage 7 artifact. Critic role, judged against `spec.md` (goal, audience,
> narrative, north-star) — NOT any phase rubric. This gates the PERFORMANCE:
> cold start, full script, presentation conditions, off-script probing. A fail
> is an escalation with a recommendation, never a silent ship.

**Conditions:** cold start ✓/✗ · seeded data ✓/✗ · resolution <res> · network <per spec>
**Timed run:** <mm:ss> against a <mm:ss> slot
**Evidence:** <screenshots per beat across the full run; timings; console log>

## The performance
| Beat | Ran clean? | Notes (pace, flow, where it dragged or dazzled) |
|------|-----------|--------------------------------------------------|
| 1    |           |                                                  |
| 3 ★  |           | <did the money moment land?>                     |

## Off-script probe
<clicked what a curious exec would click; resized; double-clicked. Anything
that dead-ends, drops to default styling, or errors:>

## Goal delivered?
<re-read the one-sentence goal: does this performance move that decision?
where short? any non-goal quietly grown?>

## Panel answer (whole demo)
"Would <this audience> lean forward at THIS performance?" <where it falls flat>

## Holistic veto
<invoked? every phase passed but the performed whole winces → FAIL + why>

## Verdict
**ACCEPT / ESCALATE.**

### If ACCEPT — demo-day runbook (hand to the presenter)
- Start: `<command>`   Reset data: `<command>`
- Click path: <the beats, tersely>
- Landmines: <known don't-click-that items, if any survived>

### If ESCALATE — recommendation
<what's flat, what it would take, and the call: cut a beat, fix, or ship-short-with-tradeoff-named>
